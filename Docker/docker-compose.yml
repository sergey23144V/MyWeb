version: '2.2'
services:
  app:
    image:
      web-post
    build:
      context: ../
      dockerfile: Docker/Dockerfile
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development

